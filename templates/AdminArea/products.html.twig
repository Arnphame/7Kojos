{% extends 'AdminArea/base.html.twig' %}
{% block body %}
    <div class="alert alert-danger alert-dismissible">
        <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
        <strong>ERROR</strong> {% if error is defined and error is not empty %}
        <div>  {{ error }} </div>
        {% endif %} }}
    </div>
    <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-4">
        <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
            <h1 class="h2">Products</h1>
        </div>
        <button onclick="location.href='{{ path('admin_add_product') }}'" type="button" class="btn btn-primary">Add new product</button>
        <br>
        <br>
    <!-- MOTHERBOARDS -->
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">Motherboards</h1>
    </div>
        <table class="table table-striped table-sm">
            <tr>
                <th>Number</th>
                <th>Name</th>
                <th>Price</th>
                <th>Discount Price</th>
                <th>Quantity</th>
                <th>Warranty</th>
                <th>Add Date</th>
                <th>Edit Date</th>
                <th>CPU Socket</th>
                <th>RAM Type</th>
                <th>Size</th>
                <th>Power cons.</th>
                <th>Edit</th>
                <th>Delete</th>
            </tr>
            {% for p in products %}
                {% for mb in mbs %}
                    {% if p.productNumber == mb.productNumber %}
                    <tr>
                        <td>{{ p.productNumber }}</td>
                        <td>{{ p.name }}</td>
                        <td>{{ p.price }}</td>
                        <td>{{ p.discountPrice }}</td>
                        <td>{{ p.quantity }}</td>
                        <td>{{ p.warrantyTime }}</td>
                        <td>{{ p.addDate|date('Y-m-d') }}</td>
                        {% if p.lastEditDate == '' %}
                            <td>N/A</td>
                        {% else %}
                            <td>{{ p.lastEditDate|date('Y-m-d') }}</td>
                        {% endif %}
                        <td>{{ mb.cpuSocket }}</td>
                        <td>{{ mb.ramType }}</td>
                        <td>{{ mb.size }}</td>
                        <td>{{ mb.powerConsumption }}</td>
                        <td><button onclick="location.href='{{ '/admin/products/edit/' ~ p.productNumber }}'" type="button" class="btn btn-primary btn-sm"><i class="fa fa-pencil" aria-hidden="true"></i></button></td>
                        <td><button onclick="location.href='{{ '/admin/products/delete/' ~ p.productNumber }}'" type="button" class="btn btn-secondary btn-sm"><i class="fa fa-trash" aria-hidden="true"></i></button></td>
                 </tr>
                {% endif %}
            {% endfor %}
         {% endfor %}
        </table>
    <!-- CPU -->
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">CPUs</h1>
    </div>
    <table class="table table-striped table-sm">
        <tr>
            <th>Number</th>
            <th>Name</th>
            <th>Price</th>
            <th>Discount Price</th>
            <th>Quantity</th>
            <th>Warranty</th>
            <th>Add Date</th>
            <th>Edit Date</th>
            <th>Socket</th>
            <th>Speed</th>
            <th>Cores</th>
            <th>Power cons.</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        {% for p in products %}
            {% for cpu in cpus %}
                {% if p.productNumber == cpu.productNumber %}
                    <tr>
                        <td>{{ p.productNumber }}</td>
                        <td>{{ p.name }}</td>
                        <td>{{ p.price }}</td>
                        <td>{{ p.discountPrice }}</td>
                        <td>{{ p.quantity }}</td>
                        <td>{{ p.warrantyTime }}</td>
                        <td>{{ p.addDate|date('Y-m-d') }}</td>
                        {% if p.lastEditDate == '' %}
                            <td>N/A</td>
                        {% else %}
                            <td>{{ p.lastEditDate|date('Y-m-d') }}</td>
                        {% endif %}
                        <td>{{ cpu.socket }}</td>
                        <td>{{ cpu.speed }}</td>
                        <td>{{ cpu.cores }}</td>
                        <td>{{ cpu.powerConsumption }}</td>
                        <td><button onclick="location.href='{{ '/admin/products/edit/' ~ p.productNumber }}'" type="button" class="btn btn-primary btn-sm"><i class="fa fa-pencil" aria-hidden="true"></i></button></td>
                        <td><button onclick="location.href='{{ '/admin/products/delete/' ~ p.productNumber }}'" type="button" class="btn btn-secondary btn-sm"><i class="fa fa-trash" aria-hidden="true"></i></button></td>
                    </tr>
                {% endif %}
            {% endfor %}
        {% endfor %}
    </table>
    <!-- GPU -->
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">GPUs</h1>
    </div>
    <table class="table table-striped table-sm">
        <tr>
            <th>Number</th>
            <th>Name</th>
            <th>Price</th>
            <th>Discount Price</th>
            <th>Quantity</th>
            <th>Warranty</th>
            <th>Add Date</th>
            <th>Edit Date</th>
            <th>Memory</th>
            <th>Core Clock</th>
            <th>Memory type</th>
            <th>Size</th>
            <th>Power cons.</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        {% for p in products %}
            {% for gpu in gpus %}
                {% if p.productNumber == gpu.productNumber %}
                    <tr>
                        <td>{{ p.productNumber }}</td>
                        <td>{{ p.name }}</td>
                        <td>{{ p.price }}</td>
                        <td>{{ p.discountPrice }}</td>
                        <td>{{ p.quantity }}</td>
                        <td>{{ p.warrantyTime }}</td>
                        <td>{{ p.addDate|date('Y-m-d') }}</td>
                        {% if p.lastEditDate == '' %}
                            <td>N/A</td>
                        {% else %}
                            <td>{{ p.lastEditDate|date('Y-m-d') }}</td>
                        {% endif %}
                        <td>{{ gpu.memory }}</td>
                        <td>{{ gpu.coreClock }}</td>
                        <td>{{ gpu.memoryType }}</td>
                        <td>{{ gpu.size }}</td>
                        <td>{{ gpu.powerConsumption }}</td>
                        <td><button onclick="location.href='{{ '/admin/products/edit/' ~ p.productNumber }}'" type="button" class="btn btn-primary btn-sm"><i class="fa fa-pencil" aria-hidden="true"></i></button></td>
                        <td><button onclick="location.href='{{ '/admin/products/delete/' ~ p.productNumber }}'" type="button" class="btn btn-secondary btn-sm"><i class="fa fa-trash" aria-hidden="true"></i></button></td>
                    </tr>
                {% endif %}
            {% endfor %}
        {% endfor %}
    </table>
    <!-- RAMs -->
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">RAMs</h1>
    </div>
    <table class="table table-striped table-sm">
        <tr>
            <th>Number</th>
            <th>Name</th>
            <th>Price</th>
            <th>Discount Price</th>
            <th>Quantity</th>
            <th>Warranty</th>
            <th>Add Date</th>
            <th>Edit Date</th>
            <th>Type</th>
            <th>Speed</th>
            <th>CAS</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        {% for p in products %}
            {% for ram in rams %}
                {% if p.productNumber == ram.productNumber %}
                    <tr>
                        <td>{{ p.productNumber }}</td>
                        <td>{{ p.name }}</td>
                        <td>{{ p.price }}</td>
                        <td>{{ p.discountPrice }}</td>
                        <td>{{ p.quantity }}</td>
                        <td>{{ p.warrantyTime }}</td>
                        <td>{{ p.addDate|date('Y-m-d') }}</td>
                        {% if p.lastEditDate == '' %}
                            <td>N/A</td>
                        {% else %}
                            <td>{{ p.lastEditDate|date('Y-m-d') }}</td>
                        {% endif %}
                        <td>{{ ram.RAMtype }}</td>
                        <td>{{ ram.speed }}</td>
                        <td>{{ ram.CAS }}</td>
                        <td><button onclick="location.href='{{ '/admin/products/edit/' ~ p.productNumber }}'" type="button" class="btn btn-primary btn-sm"><i class="fa fa-pencil" aria-hidden="true"></i></button></td>
                        <td><button onclick="location.href='{{ '/admin/products/delete/' ~ p.productNumber }}'" type="button" class="btn btn-secondary btn-sm"><i class="fa fa-trash" aria-hidden="true"></i></button></td>
                    </tr>
                {% endif %}
            {% endfor %}
        {% endfor %}
    </table>
    <!-- PSUs -->
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">PSUs</h1>
    </div>
    <table class="table table-striped table-sm">
        <tr>
            <th>Number</th>
            <th>Name</th>
            <th>Price</th>
            <th>Discount Price</th>
            <th>Quantity</th>
            <th>Warranty</th>
            <th>Add Date</th>
            <th>Edit Date</th>
            <th>Form</th>
            <th>Watts</th>
            <th>Modular</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        {% for p in products %}
            {% for psu in psus %}
                {% if p.productNumber == psu.productNumber %}
                    <tr>
                        <td>{{ p.productNumber }}</td>
                        <td>{{ p.name }}</td>
                        <td>{{ p.price }}</td>
                        <td>{{ p.discountPrice }}</td>
                        <td>{{ p.quantity }}</td>
                        <td>{{ p.warrantyTime }}</td>
                        <td>{{ p.addDate|date('Y-m-d') }}</td>
                        {% if p.lastEditDate == '' %}
                            <td>N/A</td>
                        {% else %}
                            <td>{{ p.lastEditDate|date('Y-m-d') }}</td>
                        {% endif %}
                        <td>{{ psu.form }}</td>
                        <td>{{ psu.watts }}</td>
                        <td>{{ psu.modular }}</td>
                        <td><button onclick="location.href='{{ '/admin/products/edit/' ~ p.productNumber }}'" type="button" class="btn btn-primary btn-sm"><i class="fa fa-pencil" aria-hidden="true"></i></button></td>
                        <td><button onclick="location.href='{{ '/admin/products/delete/' ~ p.productNumber }}'" type="button" class="btn btn-secondary btn-sm"><i class="fa fa-trash" aria-hidden="true"></i></button></td>
                    </tr>
                {% endif %}
            {% endfor %}
        {% endfor %}
    </table>
    <!-- Storages -->
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">Storages</h1>
    </div>
    <table class="table table-striped table-sm">
        <tr>
            <th>Number</th>
            <th>Name</th>
            <th>Price</th>
            <th>Discount Price</th>
            <th>Quantity</th>
            <th>Warranty</th>
            <th>Add Date</th>
            <th>Edit Date</th>
            <th>Size</th>
            <th>Type</th>
            <th>Form</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        {% for p in products %}
            {% for storage in storages %}
                {% if p.productNumber == storage.productNumber %}
                    <tr>
                        <td>{{ p.productNumber }}</td>
                        <td>{{ p.name }}</td>
                        <td>{{ p.price }}</td>
                        <td>{{ p.discountPrice }}</td>
                        <td>{{ p.quantity }}</td>
                        <td>{{ p.warrantyTime }}</td>
                        <td>{{ p.addDate|date('Y-m-d') }}</td>
                        {% if p.lastEditDate == '' %}
                            <td>N/A</td>
                        {% else %}
                            <td>{{ p.lastEditDate|date('Y-m-d') }}</td>
                        {% endif %}
                        <td>{{ storage.size }}</td>
                        <td>{{ storage.storageType }}</td>
                        <td>{{ storage.form }}</td>
                        <td><button onclick="location.href='{{ '/admin/products/edit/' ~ p.productNumber }}'" type="button" class="btn btn-primary btn-sm"><i class="fa fa-pencil" aria-hidden="true"></i></button></td>
                        <td><button onclick="location.href='{{ '/admin/products/delete/' ~ p.productNumber }}'" type="button" class="btn btn-secondary btn-sm"><i class="fa fa-trash" aria-hidden="true"></i></button></td>
                    </tr>
                {% endif %}
            {% endfor %}
        {% endfor %}
    </table>
    <!-- CPU Coolers -->
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">CPU Coolers</h1>
    </div>
    <table class="table table-striped table-sm">
        <tr>
            <th>Number</th>
            <th>Name</th>
            <th>Price</th>
            <th>Discount Price</th>
            <th>Quantity</th>
            <th>Warranty</th>
            <th>Add Date</th>
            <th>Edit Date</th>
            <th>RPM</th>
            <th>Noise lvl</th>
            <th>Size</th>
            <th>Type</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        {% for p in products %}
            {% for cpucooler in cpucoolers %}
                {% if p.productNumber == cpucooler.productNumber %}
                    <tr>
                        <td>{{ p.productNumber }}</td>
                        <td>{{ p.name }}</td>
                        <td>{{ p.price }}</td>
                        <td>{{ p.discountPrice }}</td>
                        <td>{{ p.quantity }}</td>
                        <td>{{ p.warrantyTime }}</td>
                        <td>{{ p.addDate|date('Y-m-d') }}</td>
                        {% if p.lastEditDate == '' %}
                            <td>N/A</td>
                        {% else %}
                            <td>{{ p.lastEditDate|date('Y-m-d') }}</td>
                        {% endif %}
                        <td>{{ cpucooler.RPM }}</td>
                        <td>{{ cpucooler.noiseLevel }}</td>
                        <td>{{ cpucooler.size }}</td>
                        <td>{{ cpucooler.CPUCoolerType }}</td>
                        <td><button onclick="location.href='{{ '/admin/products/edit/' ~ p.productNumber }}'" type="button" class="btn btn-primary btn-sm"><i class="fa fa-pencil" aria-hidden="true"></i></button></td>
                        <td><button onclick="location.href='{{ '/admin/products/delete/' ~ p.productNumber }}'" type="button" class="btn btn-secondary btn-sm"><i class="fa fa-trash" aria-hidden="true"></i></button></td>
                    </tr>
                {% endif %}
            {% endfor %}
        {% endfor %}
    </table>
    <!-- Chassis -->
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">Cases</h1>
    </div>
    <table class="table table-striped table-sm">
        <tr>
            <th>Number</th>
            <th>Name</th>
            <th>Price</th>
            <th>Discount Price</th>
            <th>Quantity</th>
            <th>Warranty</th>
            <th>Add Date</th>
            <th>Edit Date</th>
            <th>Type</th>
            <th>GPU Length</th>
            <th>Cooler sup.</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        {% for p in products %}
            {% for case in chassis %}
                {% if p.productNumber == case.productNumber %}
                    <tr>
                        <td>{{ p.productNumber }}</td>
                        <td>{{ p.name }}</td>
                        <td>{{ p.price }}</td>
                        <td>{{ p.discountPrice }}</td>
                        <td>{{ p.quantity }}</td>
                        <td>{{ p.warrantyTime }}</td>
                        <td>{{ p.addDate|date('Y-m-d') }}</td>
                        {% if p.lastEditDate == '' %}
                            <td>N/A</td>
                        {% else %}
                            <td>{{ p.lastEditDate|date('Y-m-d') }}</td>
                        {% endif %}
                        <td>{{ case.chassisType }}</td>
                        <td>{{ case.gpuLength }}</td>
                        <td>{{ case.coolerSupport }}</td>
                        <td><button onclick="location.href='{{ '/admin/products/edit/' ~ p.productNumber }}'" type="button" class="btn btn-primary btn-sm"><i class="fa fa-pencil" aria-hidden="true"></i></button></td>
                        <td><button onclick="location.href='{{ '/admin/products/delete/' ~ p.productNumber }}'" type="button" class="btn btn-secondary btn-sm"><i class="fa fa-trash" aria-hidden="true"></i></button></td>
                    </tr>
                {% endif %}
            {% endfor %}
        {% endfor %}
    </table>
    <!-- Cases coolers -->
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">Cases coolers</h1>
    </div>
    <table class="table table-striped table-sm">
        <tr>
            <th>Number</th>
            <th>Name</th>
            <th>Price</th>
            <th>Discount Price</th>
            <th>Quantity</th>
            <th>Warranty</th>
            <th>Add Date</th>
            <th>Edit Date</th>
            <th>Size</th>
            <th>RPM</th>
            <th>Airflow</th>
            <th>Noise lvl.</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        {% for p in products %}
            {% for casecooler in chassiscoolers %}
                {% if p.productNumber == casecooler.productNumber %}
                    <tr>
                        <td>{{ p.productNumber }}</td>
                        <td>{{ p.name }}</td>
                        <td>{{ p.price }}</td>
                        <td>{{ p.discountPrice }}</td>
                        <td>{{ p.quantity }}</td>
                        <td>{{ p.warrantyTime }}</td>
                        <td>{{ p.addDate|date('Y-m-d') }}</td>
                        {% if p.lastEditDate == '' %}
                            <td>N/A</td>
                        {% else %}
                            <td>{{ p.lastEditDate|date('Y-m-d') }}</td>
                        {% endif %}
                        <td>{{ casecooler.size }}</td>
                        <td>{{ casecooler.RPM }}</td>
                        <td>{{ casecooler.airflow }}</td>
                        <td>{{ casecooler.noiseLevel }}</td>
                        <td><button onclick="location.href='{{ '/admin/products/edit/' ~ p.productNumber }}'" type="button" class="btn btn-primary btn-sm"><i class="fa fa-pencil" aria-hidden="true"></i></button></td>
                        <td><button onclick="location.href='{{ '/admin/products/delete/' ~ p.productNumber }}'" type="button" class="btn btn-secondary btn-sm"><i class="fa fa-trash" aria-hidden="true"></i></button></td>
                    </tr>
                {% endif %}
            {% endfor %}
        {% endfor %}
    </table>
{% endblock %}